# Phone Connect — Catalog entry for community-plugins repository.
# Each [[plugin]] row describes a published revision. Keep newest first.

[[plugin]]
id = "icefish/phone-connect"
name = "Phone Connect"
version = "0.1.0"
plugin_api = 16
author = "icefish"
license = "MIT"
deprecated = false
icon = "devices"
description = "Control paired phones via KDE Connect — battery, ring, ping, share, clipboard, SMS, SFTP browse, media controls."
tags = ["phone", "kdeconnect", "devices", "media", "sms"]
dependencies = ["kdeconnect", "sshfs"]
